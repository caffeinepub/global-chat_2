{
  "frontend/src/App.tsx": "Root app component that checks for an active authenticated session via `getActiveSession`, renders LandingPage or ChatPage accordingly, and provides `handleLogout` to clear the session.",
  "frontend/src/pages/LandingPage.tsx": "Authentication landing page with a dual-mode form (Log In / Create Account) featuring username and password fields, show/hide password toggle, error display, and localStorage-based credential management.",
  "frontend/src/pages/ChatPage.tsx": "Main chat page now imports and uses `useServerState`, `ShutdownOverlay`, and `StartupOverlay`; renders the full-screen black shutdown overlay and green startup overlay for non-owner users only, while AI.Caffeine never sees either overlay.",
  "frontend/src/components/Sidebar.tsx": "Left sidebar showing channels, online users with verified owner badge and gold styling for 'AI.Caffeine', and a bottom user panel with logout button.",
  "frontend/src/components/TopBar.tsx": "Top navigation bar with channel info, online count, a logout button (always visible), and a conditional gold admin panel button shown only when `onAdminPanel` prop is provided.",
  "frontend/src/components/ChatArea.tsx": "Scrollable message area that now accepts `currentUsername` prop; renders `isBigMessage` messages as full-width gold announcement banners with 📢 icon, and shows a 👻 FORCED badge on `isForced` messages only when the current user is AI.Caffeine.",
  "frontend/src/components/MessageInputBar.tsx": "Auto-resizing textarea input bar that polls both ban status (red warning) and mute status (orange warning) every second; blocks sending and shows countdown for both banned and muted users, excluding the G.AI bot from checks.",
  "frontend/src/components/AIChatButton.tsx": "Floating \"G.AI Chat\" button with Bot icon in the bottom-right corner that toggles the AI chat panel.",
  "frontend/src/components/AIChatPanel.tsx": "Private G.AI chat panel that now uses OpenAI ChatGPT with full conversation memory (history array passed on every request); shows an API key setup screen if no key is stored, with a \"Change key\" option and a \"Powered by ChatGPT\" status indicator.",
  "frontend/src/components/SecretEmojiPicker.tsx": "Secret emoji picker panel (Ctrl+Alt) showing 80 fun/rare emojis in a grid. Fixed: merged conflicting `transition-colors transition-transform` into a single `transition-all` class.",
  "frontend/src/components/ConfirmationToast.tsx": "Confirmation toast for the clear-chat shortcut with auto-dismiss, confirm/cancel buttons. Fixed: replaced `flex-shrink-0` with `shrink-0`.",
  "frontend/src/types/chat.ts": "TypeScript interface for ChatMessage, now extended with optional `isBigMessage` (server-wide announcement banner) and `isForced` (admin force-sent message) fields.",
  "frontend/src/utils/sampleMessages.ts": "Utility that generates 5–8 realistic sample messages from fake users and seeds them into localStorage on first load when no messages exist.",
  "frontend/src/hooks/useBroadcastMessages.ts": "Custom hook managing real-time cross-tab messaging via BroadcastChannel + localStorage; `sendMessage` now accepts an optional `extras` parameter to attach `isBigMessage` and `isForced` flags to messages.",
  "frontend/src/hooks/useAIMentionDetector.ts": "Hook detecting `@G.ai` mentions in global chat and replying via OpenAI ChatGPT (`chatCompletion`) instead of the old Caffeine AI SDK; silently skips if no API key is configured.",
  "frontend/src/hooks/useEmojiPickerShortcut.ts": "Custom hook that listens for Ctrl+Alt keyboard shortcut to toggle the secret emoji picker panel visibility.",
  "frontend/src/hooks/useClearChatShortcut.ts": "Custom hook that listens for Ctrl+C (when no text is selected and not in an input) to show a confirmation toast, then clears local messages on confirmation.",
  "frontend/src/hooks/useQueries.ts": "Placeholder file kept for template compatibility; all data management is handled by useBroadcastMessages.",
  "frontend/src/index.css": "Global styles with Discord-inspired dark OKLCH color tokens, Inter font import, custom scrollbar styles, message slide-in animation, and AI panel slide-up animation.",
  "frontend/tailwind.config.js": "Extended Tailwind config with Discord-specific color palette (dc-bg, dc-sidebar, dc-chat, dc-input, dc-accent), Inter font family, and all shadcn/ui tokens preserved.",
  "frontend/src/lib/caffeineAI.ts": "Shared helper module that wraps the Caffeine AI SDK CDN import using `new Function` to bypass TypeScript's static module resolution, with a typed interface and module-level cache for the loaded SDK.",
  "frontend/src/lib/openai.ts": "New OpenAI integration module providing `chatCompletion` (sends a messages array to GPT-4o-mini) and `moderateMessage` (returns true if a message is disrespectful), plus helpers to get/save the API key from localStorage under `globalchat_openai_key`.",
  "frontend/src/hooks/useBanManager.ts": "Ban management hook with `isUserBanned`, `banUser`, `unbanUser`, `getRemainingBanMs`, and `getRemainingBanMinutes`; also exports `getActiveBans` for reading all current bans.",
  "frontend/src/hooks/useChatModeration.ts": "Chat moderation hook that scans incoming messages via OpenAI and bans violators; now skips messages with `isForced: true` or `isBigMessage: true` to prevent self-triggering bans on admin commands.",
  "frontend/src/lib/auth.ts": "Authentication utility module handling account creation, login validation, password encoding (btoa/encodeURIComponent), and localStorage session management; now seeds the `AI.Caffeine` owner account with password `2580` on module initialization without overwriting any existing accounts.",
  "frontend/src/lib/userBadges.tsx": "Exports `isVerifiedOwner` helper (returns true only for 'AI.Caffeine') and `VerifiedOwnerBadge` component rendering a gold crown icon with a \"Verified Owner\" tooltip.",
  "frontend/src/lib/modLog.ts": "Moderation log utility that persists up to 100 moderation events in localStorage under `globalchat_modlog`, with functions to add, retrieve, and clear log entries.",
  "frontend/src/components/AdminPanel.tsx": "Slide-out Sheet panel for AI.Caffeine with three tabs (Commands, Bans, Log); Commands tab includes Ban, Unban, Mute, Unmute actions with username inputs, a Big Message broadcast textarea, and a Force Text section with target username + message inputs, all with inline success/error feedback.",
  "frontend/src/hooks/useMuteManager.ts": "New hook providing mute management utilities (muteUser, unmuteUser, isUserMuted, getRemainingMuteMs, getRemainingMuteMinutes) backed by localStorage under `globalchat_mutes`, with automatic pruning of expired entries and a default 10-minute duration.",
  "frontend/src/hooks/useServerState.ts": "Extended server state hook that now tracks a `showStartupOverlay` boolean state, triggers it for 5 seconds when the server transitions from shutdown to running (via BroadcastChannel or natural timer expiry), and cleans up timers on unmount."
}